{
  "name": "@mindpool/e2e",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:docker": "BASE_URL=http://localhost:3000 playwright test",
    "test:meeting": "MEETING_E2E=true BASE_URL=http://localhost:3000 playwright test meeting-e2e.spec.ts",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
