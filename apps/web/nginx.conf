server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback routing — in K8s, API routing is handled by Ingress
    location / {
        try_files $uri $uri/ /index.html;
    }
}
